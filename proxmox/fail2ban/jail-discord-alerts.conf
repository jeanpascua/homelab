# Attach discord action to existing sshd jail. Drop in /etc/fail2ban/jail.d/.
# Real ban behavior (iptables/nftables) unchanged — this only ADDS a Discord
# notification alongside the normal ban.

[sshd]
action = %(action_)s
         discord[name=sshd]
